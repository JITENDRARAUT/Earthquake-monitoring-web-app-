
# 1. Initialize Git tracking structures
git init

# 2. Add files to index
git add .

# 3. Create your initialization snapshot mark
git commit -m "feat: setup complete architecture for IoT telemetry monitoring engine"

# 4. Enforce structural alignment to main branch matching your screenshot
git branch -M main

# 5. Attach remote upstream coordinate target link
git remote add origin https://github.com/JITENDRARAUT/Earthquake-monitoring-web-app-.git

# 6. Ship code to repository cloud storage
git push -u origin main --force
